type User {
  id: UUID!
  username: String! @unique
}

type Tweet {
  id: UUID!
  wallet: UUID!
  title: String!
  content: String!
  owner: User! @relation(type: REFERENCES) 
}

type Follower {
  id: UUID!
  wallet: UUID!
  owner: User! @relation(type: REFERENCES) 
}
